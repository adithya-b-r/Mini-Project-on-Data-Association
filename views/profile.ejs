<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
  <title>Mini Project 1</title>
</head>

<body>
  <div class="w-full min-h-screen bg-zinc-900 text-white p-5">
    <div class="w-full flex justify-end">
      <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">Logout</a>
    </div>

    <h3 class="text-3xl mb-5"><span class="font-light">Hello</span>, <%= user.name %> 👋🏻</h3>

    <h5 class="mb-5">You can create a new post.</h5>
    <form action="/post" method="post">
      <textarea class="w-1/3 p-3 mb-4 resize-none bg-transparent border-2 border-zinc-800 outline-none rounded-md block"
        name="content" placeholder="What's on your mind?"></textarea>
      <input class="px-3 py-2 w-40 bg-blue-500 rounded-md cursor-pointer text-sm block" type="submit"
        value="Create New Post">
    </form>

    <div class="posts mt-20">
      <h3 class="text-zinc-400">Your Posts</h3>
      <div class="postcontainer mt-5 flex flex-wrap">
        <!-- Used reverse() method to get latest posts on top. -->
        <% user.posts.reverse().forEach(function(post){ %>
        <div class="post w-1/3 p-4 m-2 rounded-md bg-zinc-800 border-[1px] border-zinc-700">
          <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
          <p class="text-sm tracking-tight"><%= post.content %></p>
          <small class="mt-2 inline-block"><%= post.likes.length %></small>
          <div class="btns flex gap-4 mt-5">
            <a class="text-blue-500" href="/like/<%= post._id %>">
              <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike"%>
            </a>
            <a class="text-zinc-400" href="/edit/<%= post._id %>">Edit</a>
          </div>
        </div>
        <% }) %>
      </div>
    </div>
  </div>

</body>

</html>